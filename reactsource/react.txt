사전 작업
node.js 설치
    npm(패키지 관리도구) == 파이썬 pip

vite : 빌드도구
    개발 중 작성한 여러 개의 파일을 하나 또는 몇개로 묶는 도구

ESLint : 문법 검사

npm create vite@latest 프로젝트명

1. vite 기반으로 react 프로젝트 생성하기

npm run dev : 작성하는 코드가 실시간으로 반영됨(개발 시 사용)

개발 종료 후 build

npm run build : 서버에 올려서 사용자들에게 웹페이지로 배포될 파일들로 코드를 모아 컴파일
dist 폴더 생성

npm run preiview : build 를 통해 나온 결과물을 테스트하는 단계
http://localhost:4173/

jsx(javascript xml)
tsx(typescript xml)

정리 명령어 = shift + alt + o


기본 : 부모가 렌더링되면 자식도 렌더링 됨
자식 컴포넌트의 state 값이 변경이 되면 자식 컴포넌트만 리렌더링

부모가 렌더링 될때 자식의 컴포넌트에 영향을 끼치지 않는다면 
자식의 렌더링을 막는게 효율적 => React.memo()

npm create vite@latest 프로젝트명
npm install tailwindcss @tailwindcss/vite
npm install react-router-dom

라우팅 : 링크(페이지 이동)
<a href = 'index.html'> 인덱스 </a>

컴포넌트

링크 관련
a: html 태그
-새로고침 되면서 화면의 깜빡임 발생
NavLink, link : React-router-dom
-새로고침 없이 화면이 변경됨

NavLink : active 클래스가 자동으로 부여
- 상단의 URL과 NavLink 의 to 속성값 일치시

Outlet
- 중첩 라우팅에서 자식 라우트를 렌더링할 위치를 지정
- 공통으로 사용할 레이아웃 지정

리액트에서는 use~~~: 훅
훅: React가 제공하는 특별 함수
- React의 기능을 사용할 수 있음
- 컴포넌트의 최상위에서만 호출 가능
- React의 상태, 생명주기 등의 기능과 연결

https://reactrouter.com/start/declarative/installation


useParams / useLocation

useLocation
- 현재 페이지의 전체 url 정보를 가져올 수 있는 훅
- pathname(현재경로)
- search(쿼리스트링) : 문자열형태

useSearchParams
- 현재 페이지의 쿼리스트링을 개별항목으로 가져오거나 수정할 수 있는 훅

useParams
- 동적으로 생성되는 파라미터 가져오기


/account/login
/account/register
/account/profile/5 + GET:3번 유저의 profile 가져와
컴포넌트 생명 주기:useEffect 훅
마운트(화면에 그려짐), 업데이트(상태(state), 속성(props)변경되어 다시 렌더링), 언마운트(화면에서 사라짐)

서버에서 데이터 가져오기(json)

아이콘 활용
- react icons
- 구글 아이콘
- font awesome